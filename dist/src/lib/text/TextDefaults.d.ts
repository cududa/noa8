/**
 * Default options for text creation.
 * @param {object} constructorOpts - Options from Text constructor
 * @returns {object} Default text creation options
 */
export function createDefaultOptions(constructorOpts: object): object;
export namespace CONSTRUCTOR_DEFAULTS {
    let defaultFont: string;
    let scale: number;
    let enabled: boolean;
}
